version: '3.7'

services:
  ffc-jenkins-pipeline-library-semantic-release:
    build:
      context: .
      dockerfile: semantic-release.Dockerfile
    entrypoint: ./scripts/test
    # entrypoint: /bin/bash
    environment:
      GH_TOKEN: $GH_TOKEN
    image: ffc-jenkins-pipeline-library-semantic-release
    tty: true
    volumes:
      - ./scripts/:/home/node/wrk/scripts
      # - ./:/home/node/wrk/
